const FuelCell = require ('./fuelcell');

(function () {
   'use strict';

   function Grid(width, height) {
       this.addFuelCell();
   }

   Grid.prototype.addFuelCell = function (x, y, fuelcell) {

    };

   module.exports = Grid;
});